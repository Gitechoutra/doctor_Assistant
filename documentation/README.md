# Documentation

Setup and run instructions live in the [project root README](../README.md).

This folder will hold deeper docs (API reference, DB schema notes,
deployment guide) as later milestones add the features they document.
